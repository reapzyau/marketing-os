# Client Registry

This file holds pointers only. Client work lives in each client's own repo, never
here — the repo is the access boundary, so this registry records where each client's
brain is and who may reach it.

Status values: active, paused, offboarded.

| Client | Repo | Status | Access |
| --- | --- | --- | --- |
| _example-client_ | `../example-client` | active | you |
